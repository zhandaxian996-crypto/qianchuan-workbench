# Agent 知识接入

先按 [启动手册](AGENT_START.md) 完成环境检查与只读接入，再使用随包技能和工作区形成分析。程序、方法、用户参数缺一不可；技能本身不带任何已连接账户。

## 从这里读

1. 阅读 [千川技能入口](skills/qianchuan-ops/SKILL.md) 与 [接入说明](skills/qianchuan-ops/references/接入与执行.md)。资料路径以技能文件位置为基准。是否安装到技能目录，按当前 Agent 客户端的真实规则处理；支持直接读文件时即可先用，不臆造通用安装命令。
2. 阅读 [工作区规则](workspace/AGENTS.md)、[工作区索引](workspace/工作区索引.md) 和 [只读分析入口](workspace/运行入口/只读分析.md)。客户已有工作区时建立路径映射并沿用，不覆盖其规则或已填写资料。
3. 按当前问题读取相关方法卡和规则证据。首次分析不必加载整库，后续只在内容缺失或版本变化时重读。
4. 核对当前 MCP Schema。接口语义见 [技术契约](docs/agent-contracts/README.md)，工具真实返回和服务器限制优先用于判断当前能力。

## 谁提供什么

| 内容 | 来源与保存位置 |
|---|---|
| 通用规则、公式和操作方法 | 随包技能；保持通用，不填入客户数据 |
| 数据读取、任务接续和分析流程 | 随包通用工作区；用户已有工作区时按映射使用 |
| 账户身份和登录状态 | 客户本人在本机工作台接入；凭据不写入聊天、模板或分享包 |
| 经营目标、预算、保本口径和禁止事项 | 客户自己的当前策略；Agent 整理已有事实，只询问关键缺口 |
| 新任务、素材分析和复盘 | 接入后由客户自己的 Agent 在本地形成；初始交付没有这些历史数据 |

模板是空白结构，未知项保留未知。历史方法中的示例、来源日期和研究结论不构成当前平台规则或自动操作权限；涉及实时规则的决定应按技能要求重新核验。

## 本包可用范围

本包维持 `recommendation_only`，关闭投放写入与后台持续任务。技能和技术契约可能解释完整产品的写入能力；这属于方法资料，不能据此切换模式、绕开启动器或开启心跳。先完成一次有来源时间、指标口径和明确缺口的只读分析。

首次验收要同时说明：环境是否就绪、客户账户是否确实接入、数据是否新鲜、方法依据是否可找到，以及仍缺哪些客户参数。资料齐全不能代替真实账户验证。
